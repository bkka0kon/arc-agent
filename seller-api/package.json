{
  "name": "arc-agentic-seller-api",
  "version": "0.1.0",
  "description": "Sample seller-side API for Arc Agentic — pay-per-call in USDC via Circle Gateway on Arc Testnet",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  },
  "engines": {
    "node": ">=20"
  }
}
